spring:
    application:
        name: Eureka-server

server:
    port: 8761

eureka:
    client:
        register-with-eureka: false
        fetch-registry: false
logging:
    level:
        com.netflix.eureka: debug
        com.netflix.discover: debug
